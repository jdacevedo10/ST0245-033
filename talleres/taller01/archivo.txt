import java.util.*;

class Main {
  public static void main(String[] args) {
   /*Punto a = new Punto(6,3);
   System.out.println("radio: "+ a.radio()); **/
   /*Date b = new Date(10,02,2015);
   System.out.println(b.toString());**/
   Contador c = new Contador();
   System.out.println(c.incrementar(5,5));
  }
}
   class Punto 
{
  private double x;
  private double y;

    public Punto(double X, double Y)
    {
      this.x=X;
      this.y=Y;

    }

    public double CordenadaX()
    {
      return this.x;
    }

    public double CordenadaY()
    {
      return this.y;
    }

    public double radio()
    {
      return Math.sqrt(x*x + this.y*this.y);
    }

    public double angulo()
    {
      return  Math.tan(y/x);
    }
    public double distance(Punto punto){
      double otrax = this.x - punto.x ;
      double otray = this.y - punto.y ;
      return Math.sqrt (otrax * otrax + otray * otray) ;
    }
 }
 class Date {

 private int day, mon, year;
 
 public Date(int day, int mon, int year)
 {
     this.day = day; 
     this.mon = mon; 
     this.year = year;
 }
 
 public int day()
 {
     return this.day;
 }
 
 public int mon()
 {
     return this.mon;
 }
 
 public int year()
 {
     return this.year;
 }
 
 public int comparar(Date otra)
 {
     if (this.year < otra.year) {
         return -1;
     }
     
     if (this.year > otra.year) {
         return 1;
     }
     
     if (this.mon < otra.mon) {
         return -1;
     }
     
     if (this.mon > otra.mon) {
         return 1;
     }
     
     if (this.day < otra.day) {
         return -1;
     }
     
     if (this.day > otra.day) {
         return 1;
         
     }
     return 0;
 }
 
 public String toString()
 {
     return day+" / "+mon+" / "+year;
 }
 }

 class Contador {
    private int cuenta;
    private final String id;

    public Contador(String id) {
        this.id = id;
        cuenta = 0;
    }
    
    public int incrementos(int a, int b)
    {
        for (int i = 0; i < b; i++) {
            cuenta = cuenta+a;
        }
        return cuenta;
    }
    
    public int incremento()
    {
        return cuenta; 
    }
    
    public String toString()
    {
        return "ID: "+id+" "+cuenta;
    }
    
    
}


